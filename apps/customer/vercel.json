{
  "installCommand": "cd ../.. && yarn install",
  "buildCommand": "cd ../.. && yarn run build:web",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
