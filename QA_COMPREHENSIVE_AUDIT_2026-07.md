# 🧾 تقرير الفحص الشامل قبل الإطلاق — تطبيق المتجر (Marketplace)

**تاريخ الفحص:** 2026-07-02
**النسخة المفحوصة:** فرع `claude/ecommerce-qa-comprehensive`
**نوع التطبيق:** تطبيق تجارة إلكترونية متعدد الأدوار (عميل / تاجر / مندوب / إدارة) — React Native + Expo SDK 54، خلفية Supabase (PostgreSQL + Auth + Storage).
**المُقيّم:** فحص QA / أمن / منطق أعمال / تجربة مستخدم.

---

## ⚠️ منهجية الفحص وحدوده (اقرأ هذا أولاً)

هذا الفحص **فحص كود ثابت (Static Analysis)** لملفات المشروع الفعلية: طبقة الـ API (`packages/shared-hooks/src/api.ts`)، مخزن المصادقة (`useAuthStore.ts`)، مخطط قاعدة البيانات و سياسات الأمان (`supabase/migrations/*.sql`)، وشاشات التطبيق (`apps/customer/src/screens/**`). **لم يتم تشغيل التطبيق فعلياً على جهاز** لأن البيئة الحالية لا توفّر جهاز محاكاة ولا مفاتيح Supabase حيّة.

**تحذير مهم حول قاعدة البيانات:** ملفات الـ migration في المستودع **ناقصة وغير متطابقة مع قاعدة البيانات الحيّة**. الدليل: ملف `20260619000000_secure_rls_lockdown_and_insert_fixes.sql` يُفعّل RLS على 55 جدولاً (`admin_roles`, `promotions`, `loyalty_rules`, `fraud_flags`, …) **لا وجود لأيٍّ منها في ملف المخطط الأساسي** `20260610000000_initial_schema.sql`. هذا يعني أنه **لا يمكن إعادة بناء قاعدة البيانات من المستودع** — وهي بحد ذاتها مشكلة تشغيلية حرجة. لذلك بعض ملاحظات "عدم تطابق الأعمدة" أدناه قد تكون مُصلَحة في القاعدة الحيّة عبر migrations غير موجودة في المستودع؛ لكنها تبقى **مخاطر مؤكدة على مستوى المصدر** يجب التحقق منها يدوياً على القاعدة الحيّة.

---

## 🎯 الخلاصة التنفيذية

| البند | التقييم |
|---|---|
| **نسبة الجاهزية للإطلاق** | **≈ 38 / 100** |
| **هل هو جاهز للإطلاق؟** | ❌ **لا — إطلاقه الآن خطر قانوني وأمني مباشر** |
| **الحالة الحقيقية** | هيكل واجهات واسع وجميل (44 شاشة) فوق طبقة **مصادقة مكسورة أمنياً** و**طبقة بيانات لا تتطابق مع قاعدتها** |
| **أخطر مشكلة** | كلمة مرور كل مستخدم = دالة معروفة من رقم هاتفه → **الاستيلاء على أي حساب لا يحتاج سوى معرفة رقم الجوال** |

التطبيق يبدو من الخارج مكتملاً (شاشات كثيرة، هوية بصرية موحّدة، RTL، حالات فارغة). لكن تحت السطح توجد **6 ثغرات حرجة (Critical)** أيٌّ منها كافٍ وحده لمنع الإطلاق، إضافة إلى عدم تطابق واسع بين الكود والمخطط سيجعل شاشات كاملة تتعطّل فور الاتصال بقاعدة بيانات حقيقية.

---

## 🔴 1) الثغرات الأمنية الحرجة (Critical) — لا إطلاق قبل حلّها

### C-1 — انهيار المصادقة: كلمة المرور دالة معروفة من رقم الهاتف (استيلاء كامل على الحسابات)
**الملف:** `packages/shared-hooks/src/useAuthStore.ts`
```ts
const phoneToEmail    = (phone) => `u${phoneDigits(phone)}@levi-phone.app`;
const phoneToPassword = (phone) => `Levi-${phoneDigits(phone)}-auth`;
```
التطبيق يحوّل رقم الهاتف إلى بريد وكلمة مرور **حتميّين ومكشوفين**. أي شخص يعرف رقم جوال ضحية (أو يجرّب أرقاماً تسلسلياً) يستطيع تسجيل الدخول كاملاً باسمها عبر `signInWithPhone` — بدون OTP، بدون كلمة سر، بدون أي تحقق. **هذه أخطر ثغرة في المشروع**: كل حساب (عميل، تاجر، مندوب، بل وأدمن) قابل للاختراق فورياً. تسريب رقم جوال واحد = اختراق حساب.
**الأثر:** استيلاء جماعي على الحسابات، سرقة محافظ التجار/المندوبين، تنفيذ طلبات باسم الآخرين، تسريب بيانات شخصية.

### C-2 — لا يوجد تحقق OTP إطلاقاً (شاشة الرمز ديكور)
`OtpScreen.handleVerify` يستدعي `verifyOtp(phone, token, type:'sms')` بينما لا يوجد مزوّد SMS مُفعّل، فتفشل دوماً؛ وزر "إعادة الإرسال" يستدعي `signInWithPhone` الذي يسجّل الدخول **بدون رمز**. أي أن "التحقق من البريد/الهاتف" المطلوب **غير موجود فعلياً**. المستخدم إمّا يعلق على شاشة الرمز أو يدخل بدون تحقق.

### C-3 — تصعيد صلاحيات: أي مستخدم يجعل نفسه Admin
سببان مستقلان، كلٌّ منهما كافٍ:
1. **عند التسجيل:** `signUp` يمرّر `role` من واجهة العميل ضمن `user_metadata`، و trigger `handle_new_user` يثق به: `COALESCE((raw_user_meta_data->>'role')::user_role, 'customer')`. تعديل بسيط للطلب → `role: 'admin'`.
2. **بعد التسجيل:** سياسة `users` للتحديث `WITH CHECK (auth.uid() = id)` **تسمح بتعديل عمود `role`** (التعليق في الكود يقول "يجب منعه عبر trigger" لكن لا trigger موجود). أي `supabase.from('users').update({ role:'admin' })` ينجح.
**الأثر:** سيطرة كاملة على لوحة الإدارة، البيانات، الأموال.

### C-4 — أعمدة/جداول حساسة بلا RLS (تسريب بيانات شخصية + تلاعب)
جدول `addresses` **لم يُفعَّل عليه RLS** لا في `20260612000000_initial_rls.sql` ولا في migration التحصين. في Supabase، أي جدول بلا RLS + منح PostgREST الافتراضي = **مقروء ومكتوب للجميع بمفتاح anon**. النتيجة: أي شخص يقرأ **كل عناوين العملاء** (سكن + إحداثيات GPS)، ويعدّل/يحذف عناوين أي أحد. جداول أخرى في نفس الوضع (غير مذكورة في أي `ENABLE ROW LEVEL SECURITY`): `customer_profiles`, `product_variants`, `product_images`, `notifications`, `wishlists`, `store_follows`, `carts`, `cart_items`, `chat_conversations`, `chat_messages`, `refund_requests`, `complaints`, `order_tracking`, `advertisements`, `inventory_logs`, `platform_settings`.
**الأثر:** انتهاك خصوصية جسيم (PII)، قراءة محادثات الآخرين، قراءة إشعاراتهم، العبث بالمخزون والإعدادات.

### C-5 — التاجر يعتمد متجره بنفسه ويثبّت منتجاته في المميّزة
سياسة `merchant_profiles` للتحديث: `USING (auth.uid() = id)` **بدون `WITH CHECK`**، فيستطيع التاجر ضبط `is_approved = true` (تجاوز مراجعة الإدارة)، و`commission_rate = 0` (إلغاء عمولة المنصة)، و`rating = 5`. كذلك سياسة `products` هي `FOR ALL USING (auth.uid() = merchant_id)` بلا `WITH CHECK` مقيِّد، فيستطيع التاجر ضبط `is_featured = true` و`is_approved = true` على منتجاته. والأسوأ: **العرض العام للمنتجات يعتمد على `is_active` فقط** (`Public read active products`) ولا يفحص `is_approved` أصلاً → مراجعة المنتجات بلا أثر عملي.
**الأثر:** خسارة عمولات مباشرة، إغراق الواجهة بمنتجات غير مُراجَعة، تلاعب بالتقييم.

### C-6 — الأسعار محسوبة ومُرسَلة من العميل (تلاعب بالمبالغ)
**الملف:** `CheckoutScreen.tsx` + `api.ts:createOrder`. العميل يحسب `total_amount = subtotal + deliveryFee - discount` من أسعار محفوظة في السلة على الجهاز، ويرسلها كما هي إلى `orders`. الخادم **لا يعيد حساب أي شيء** ولا يتحقق من سعر المنتج الحقيقي ولا من رسوم التوصيل. عميل خبيث يرسل `total_amount: 0`.
**الأثر:** شراء بمبالغ صفرية/مزيّفة، خسارة مالية مباشرة، اختلال المحاسبة والعمولات.

---

## 🟠 2) أخطاء ربط الكود بقاعدة البيانات (High) — شاشات ستتعطّل فور التشغيل الحقيقي

هذه أخطاء "سيلقيها PostgREST وقت التشغيل" لأن الـ API يطلب أعمدة/جداول لا يعرّفها المخطط. (بعضها قد يكون مُصلَحاً في القاعدة الحيّة — راجع تحذير المنهجية.)

| # | الموقع | العطل | النتيجة على المستخدم |
|---|---|---|---|
| H-1 | `getFeaturedProducts` / `searchProducts` تطلب `products.stock_quantity` | العمود غير موجود في `products` (المخزون في `product_variants.stock_quantity`) | **الرئيسية والبحث يرجعان خطأ** |
| H-2 | `validateCoupon` / `getActiveCoupons` تطلب `coupons.type,value,max_discount_amount,end_date` | المخطط يعرّف `discount_type, discount_value, expires_at` ولا يوجد `max_discount_amount` | **الكوبونات لا تعمل نهائياً** |
| H-3 | سياسة `coupons` هي `FOR ALL USING (false)` | العميل ممنوع من قراءة الكوبونات أصلاً | حتى لو صحّت الأعمدة، `validateCoupon` تُرجع "غير صحيح" دائماً |
| H-4 | `getOrCreateConversation`/`getConversations` تستخدم `customer_id, merchant_id, last_message, customer_unread…` | المخطط يعرّف `chat_conversations(participant_ids[], order_id)` فقط | **المحادثات معطّلة كلياً** |
| H-5 | `sendMessage`/`getMessages` تستخدم `chat_messages.message, message_type` | المخطط يعرّف `body` وليس `message` | **إرسال/قراءة الرسائل يفشل** |
| H-6 | `getReviews`/`createReview` تستخدم `reviews.reviewer_id, target_id, target_type` | المخطط يعرّف `customer_id, merchant_id, product_id, order_id` | **التقييمات معطّلة** |
| H-7 | `getProductVariants` تطلب `size, color, color_hex, additional_price, stock_qty` | المخطط يعرّف `name, price_modifier, stock_quantity` (تعريفان متناقضان للـ variant في نفس ملف api.ts!) | خيارات المنتج معطّلة |
| H-8 | `getMerchantWalletBalance`/`createMerchantProfile` تستخدم `wallet_balance, tax_number, owner_name, national_id, store_phone, whatsapp, bank_account_name` على `merchant_profiles` | لا وجود لهذه الأعمدة في المخطط | **إنشاء متجر ومحفظة التاجر يفشلان** |
| H-9 | `createRefundRequest` يُدرِج `refund_amount, description` | المخطط يعرّف `amount` فقط | طلب الاسترجاع يفشل |
| H-10 | جداول يطلبها الـ API وغير موجودة في أي migration: `support_tickets`, `support_messages`, `loyalty_transactions`, `cancellation_reasons`, `merchant_working_hours`, `service_areas`, `delivery_earnings`, `saved_payment_methods` | معظمها مغلّف بـ try/catch يُرجع `[]` | الدعم/الولاء/أسباب الإلغاء/ساعات العمل/أرباح المندوب/طرق الدفع **تظهر فارغة صامتة** |
| H-11 | تضمين `orders → merchant_profiles(store_name)` عبر `merchant_id` | مفتاح `orders.merchant_id` يشير إلى `users` لا إلى `merchant_profiles`؛ قد لا يحلّه PostgREST | اسم المتجر في قائمة الطلبات قد يفشل |

---

## 🟠 3) أخطاء منطق الأعمال ودورة الطلب (High)

- **لا سياسات INSERT/UPDATE على `orders`:** في ملفات RLS لا توجد سياسة INSERT ولا UPDATE على `orders`. مع تفعيل RLS + الرفض الافتراضي، فإن **إنشاء الطلب من العميل مباشرة، وقبول/رفض التاجر (`updateOrderStatus`)، والإلغاء (`cancelOrder`) — جميعها ستُرفض** إلا إن كانت القاعدة الحيّة تحوي سياسات غير موجودة في المستودع. يجب تمرير هذه العمليات عبر RPC آمنة (SECURITY DEFINER) مع تحقق من ملكية الطلب وصحة الانتقال.
- **لا آلة حالات (State Machine) للطلب:** `updateOrderStatus(orderId, status)` تقبل **أي نص حالة** بلا تحقق. لا يوجد ما يمنع القفز من `pending` مباشرة إلى `delivered`، أو إعادة تنشيط طلب ملغى. دورة الطلب المطلوبة (إنشاء → قبول → تجهيز → إسناد → استلام → توصيل → تسليم → إغلاق) غير مفروضة على الخادم.
- **المخزون غير ذرّي وغير محقّق:** `createOrder` يُدرج الطلب ثم الأصناف ثم يستدعي `decrement_product_stock` في 3 مكالمات منفصلة **بلا معاملة (transaction)**. لا يوجد فحص توفّر المخزون قبل الطلب → **بيع بالسالب (Overselling)** عند التزامن، وحالة بيانات غير متسقة عند فشل جزئي (طلب موجود بلا خصم مخزون أو العكس). كما أن `products` لا يملك عمود مخزون أصلاً (المخزون على مستوى variant).
- **الكوبون بلا حدود استخدام:** لا فحص لـ `max_uses` ولا `coupon_usage` لكل مستخدم؛ لو عملت الكوبونات لأمكن استخدام نفس الكود بلا حد. (عملياً معطّلة الآن بسبب H-2/H-3.)
- **اختيار العنوان مكسور:** `CheckoutScreen` يستخدم حقلاً نصياً حراً وينشئ عنواناً جديداً في كل عملية شراء (`createAddress` في كل مرة) بدل الاختيار من دفتر العناوين المحفوظ → **تكرار العناوين** وتجاهل شاشة `AddressBook`.
- **السلة متعددة المتاجر:** عند وجود منتجات من متاجر مختلفة يُنشأ طلب لكل متجر لكن **تُضاف رسوم التوصيل كاملة لكل متجر** ويُقسَّم الخصم — منطق تسعير قابل للجدل ويجب توثيقه/تصحيحه.
- **السلة غير محفوظة وغير مزامنة:** `useCartStore` في الذاكرة فقط (Zustand بلا persist) → **تُفقد السلة عند إغلاق التطبيق**، ولا تُكتب إلى جدولي `carts/cart_items` الموجودين أصلاً.
- **الاسترجاع/الاستبدال/رفض الاسترجاع/استرداد الأموال:** يوجد `createRefundRequest` فقط (ومكسور H-9). لا يوجد مسار موافقة/رفض، ولا استبدال، ولا ربط باسترداد فعلي للمحفظة، ولا طلبات جزئية (`partial_delivery` معرّفة كحالة لكن بلا منطق).

---

## 🟡 4) فحص حسب الدور

### العميل
- **الرئيسية/البحث:** يتعطّلان بسبب `stock_quantity` (H-1). **لا يوجد Pagination** — كل الاستعلامات `.limit(30/50)` ثابتة بلا تحميل تدريجي (مشكلة أداء عند نمو البيانات).
- **البحث الصوتي:** **غير موجود إطلاقاً** رغم طلبه (لا استيراد `expo-speech`/تسجيل صوتي في أي شاشة).
- **المفضلة/الإشعارات:** الاستعلامات موجودة لكن الجداول بلا RLS (C-4) → قابلة للقراءة العامة.
- **الكوبونات/العروض:** معطّلة (H-2, H-3).
- **الدفع:** طريقة دفع واحدة فعلية (COD)؛ البوابات الإلكترونية (بطاقات/محافظ يمنية jawali/kuraimi…) معرّفة كـ enum فقط بلا تكامل بوابة دفع حقيقي.
- **حذف الحساب:** **غير موجود** — لا API ولا شاشة. (مطلب إلزامي لمتجري Apple/Google.)
- **اللغة/الثيم:** `STORAGE_KEYS` تعرّف مفاتيح لغة وثيم لكن **لا يوجد مبدّل لغة فعلي ولا وضع ليلي مطبّق** — التطبيق يفرض RTL ثابتاً (`forceRTL(true)`) بلا خيار للإنجليزية.

### التاجر
- **إنشاء/تعديل المتجر:** يفشل بسبب أعمدة غير موجودة (H-8).
- **إضافة منتج:** `createProduct` لا يضبط مخزوناً ولا يرفع صوراً (رفع الصور موجود كدالة `uploadImageToStorage` لكن الأدلّة تشير إلى أن الشاشات تستخدم أيقونات).
- **الأرباح/التقارير/المحفظة:** تعتمد `wallet_balance` غير الموجود و`delivery_earnings`/جداول مفقودة → أرقام صفرية أو أخطاء.
- **إدارة الموظفين/الصلاحيات:** غير موجودة في كود العميل.

### المندوب
- **قبول الطلب:** عبر RPC `claim_delivery_order` (تصميم سليم، SECURITY DEFINER) — لكن **جسم الدالة غير موجود في المستودع** فلا يمكن التحقق من صحة منطق الإسناد/التزامن.
- **الملاحة/تحديث الموقع/إثبات التسليم:** الخريطة "placeholder"؛ لا تكامل خرائط حقيقي، ولا كتابة `current_latitude/longitude`، ولا رفع إثبات تسليم فعلي (`delivery_proofs` عليه RLS لكن بلا منطق ربط).
- **الأرباح:** تعتمد `delivery_earnings` المفقود (H-10).

### الإدارة (Admin)
- **لا توجد لوحة إدارة في هذا التطبيق إطلاقاً.** التطبيق يوجّه دور `admin` إلى واجهة العميل (`MainTabNavigator`) في `App.tsx`. جميع بنود "إدارة المستخدمين/المتاجر/العمولات/البلاغات/الصلاحيات/النسخ الاحتياطي/السجلات" **غير منفّذة**. الجداول موجودة (`admin_roles`, `admin_activity_logs`, `fraud_flags`…) لكن بلا أي واجهة أو API.

---

## 🟡 5) تجربة المستخدم (UX) والواجهة (UI)

- **أزرار ميتة:** زرّا "الدخول عبر Apple/Google" في `LoginScreen` بلا `onPress` — يبدوان فعّالين لكن لا يفعلان شيئاً (خداع للمستخدم + رفض محتمل من المتاجر).
- **شاشة OTP مضلِّلة:** تطلب رمزاً لن يصل أبداً.
- **RTL:** مطبّق جيداً وثابت. لكن **لا دعم للإنجليزية** رغم وجود مفتاح لغة.
- **الوضع الليلي:** غير مطبّق (`COLORS` ثابتة فاتحة).
- **الاستجابة/الأجهزة اللوحية:** التصميم قائم على قيم ثابتة (`SPACING/FONT_SIZE`) بلا breakpoints؛ يحتاج فحصاً على الأجهزة اللوحية والشاشات الصغيرة.
- **حالات الخطأ:** كثير من دوال الـ API تبتلع الأخطاء وتُرجع `[]` بصمت (support/loyalty/reviews...) → المستخدم يرى شاشات فارغة بلا تفسير بدل رسالة خطأ واضحة.
- **الإيجابيات:** هوية بصرية موحّدة فعلاً، حالات فارغة/تحميل في الشاشات الرئيسية، تدفّق تنقل واضح للأدوار الثلاثة.

## 🟡 6) الأداء
- **لا Pagination حقيقي** (حدود ثابتة). **لا Lazy Loading للصور** موثّق. **الكاش** غير موجود (لا React Query/SWR؛ كل شاشة تجلب مباشرة). المحفظة/المؤشرات تجلب كل السجلات ثم تجمّع في العميل (`getMerchantSalesChart` يجلب كل الطلبات ويجمّع محلياً — يكبر مع الوقت).

## 🟡 7) الإشعارات
- جدول `notifications` و`device_tokens` موجودان، لكن **لا يوجد تكامل Push فعلي** (`expo-notifications` غير مربوط بمنطق إرسال). trigger `notify_order_events` مذكور في قائمة الصلاحيات لكن جسمه غير موجود في المستودع. عملياً: **لا إشعارات فورية تصل لأي دور**.

---

## 📊 8) تصنيف كل المشاكل حسب الخطورة

| المعرّف | الخطورة | المشكلة |
|---|---|---|
| C-1 | 🔴 Critical | كلمة المرور حتميّة من رقم الهاتف → استيلاء كامل على الحسابات |
| C-2 | 🔴 Critical | لا تحقق OTP فعلي |
| C-3 | 🔴 Critical | تصعيد صلاحيات إلى admin (عبر التسجيل + عبر UPDATE) |
| C-4 | 🔴 Critical | جداول PII بلا RLS (addresses وغيرها) — تسريب وتلاعب |
| C-5 | 🔴 Critical | التاجر يعتمد متجره ويلغي العمولة ويميّز منتجاته |
| C-6 | 🔴 Critical | الأسعار من العميل بلا إعادة حساب على الخادم |
| H-1..H-11 | 🟠 High | عدم تطابق الكود مع مخطط قاعدة البيانات (شاشات تتعطّل) |
| H-12 | 🟠 High | لا سياسات INSERT/UPDATE على orders (دورة الطلب مرفوضة/غير آمنة) |
| H-13 | 🟠 High | لا آلة حالات للطلب |
| H-14 | 🟠 High | المخزون غير ذرّي + بيع بالسالب |
| H-15 | 🟠 High | migrations ناقصة/غير قابلة لإعادة البناء |
| H-16 | 🟠 High | لا لوحة إدارة |
| M-1 | 🟡 Medium | السلة غير محفوظة/غير مزامنة |
| M-2 | 🟡 Medium | اختيار العنوان ينشئ تكراراً بدل الاختيار |
| M-3 | 🟡 Medium | لا حذف حساب (مطلب المتاجر) |
| M-4 | 🟡 Medium | أزرار Apple/Google ميتة |
| M-5 | 🟡 Medium | لا Push فعلي |
| M-6 | 🟡 Medium | لا Pagination/كاش/Lazy loading |
| M-7 | 🟡 Medium | مسار استرجاع/استبدال/استرداد ناقص |
| L-1 | 🟢 Low | لا وضع ليلي |
| L-2 | 🟢 Low | لا مبدّل لغة (RTL مفروض) |
| L-3 | 🟢 Low | لا بحث صوتي |
| L-4 | 🟢 Low | ابتلاع الأخطاء بصمت (UX) |
| L-5 | 🟢 Low | `refreshUser` قد يُخرج مستخدماً صالحاً عند تعارض إدراج |

---

## 🛠️ 9) خطة الإصلاح مرتّبة حسب الأولوية

**المرحلة 0 — إيقاف النزيف الأمني (قبل أي شيء آخر):**
1. استبدال المصادقة الحتميّة بـ **OTP حقيقي عبر مزوّد SMS** (Twilio/مزوّد يمني) أو Supabase Phone Auth؛ حذف `phoneToPassword` نهائياً.
2. منع تعيين `role` من العميل: تجاهله في `handle_new_user`، وإضافة **trigger يمنع تعديل `role/is_active/is_verified`** في `users`، وإضافة `WITH CHECK` صارم.
3. **تفعيل RLS على كل جدول** (addresses أولاً) مع سياسات ملكية دقيقة؛ تدقيق شامل عبر `pg_tables` + Supabase Advisor.
4. `WITH CHECK` على `merchant_profiles` يمنع تعديل `is_approved/commission_rate/rating`؛ ونقل `is_approved` للمنتجات لمسار الإدارة، وجعل العرض العام يشترط `is_approved`.
5. نقل **إنشاء الطلب وحساب المبلغ** إلى **RPC/Edge Function** تعيد حساب الأسعار من الخادم، تتحقق من المخزون ذرّياً داخل معاملة واحدة، وتطبّق الكوبون وحدوده على الخادم.

**المرحلة 1 — إصلاح طبقة البيانات:**
6. توحيد أسماء الأعمدة بين `api.ts` والمخطط (H-1..H-11)، وإنشاء الجداول المفقودة (H-10)، وكتابة **migrations كاملة قابلة لإعادة البناء** تطابق القاعدة الحيّة (H-15).
7. آلة حالات للطلب (انتقالات مسموحة فقط) + سجل `order_tracking`.

**المرحلة 2 — اكتمال الوظائف:**
8. لوحة إدارة فعلية (ولو ويب منفصلة). 9. مسار استرجاع/استبدال/استرداد كامل. 10. Push notifications فعلية. 11. حذف الحساب. 12. مزامنة السلة + اختيار العنوان من الدفتر. 13. بوابة دفع حقيقية.

**المرحلة 3 — الجودة:**
14. Pagination + كاش (React Query) + Lazy images. 15. وضع ليلي + مبدّل لغة. 16. رسائل خطأ واضحة. 17. اختبارات آلية (وحدة + E2E) وCI.

---

## ✅ 10) Checklist ما قبل الإطلاق

- [ ] OTP/مصادقة حقيقية بلا أي backdoor، وحذف الرموز التجريبية (`123456/111111/222222`)
- [ ] لا يمكن لأي عميل تعيين/تعديل دوره
- [ ] RLS مفعّل ومختبَر على **كل** جدول (خاصة PII)
- [ ] التاجر لا يستطيع اعتماد نفسه أو تصفير العمولة
- [ ] المبالغ والمخزون والكوبونات محسوبة/مفروضة على الخادم داخل معاملة
- [ ] migrations كاملة تعيد بناء القاعدة من الصفر بنجاح
- [ ] آلة حالات للطلب + مسار استرجاع/استرداد كامل
- [ ] لوحة إدارة تعمل
- [ ] Push notifications تصل للأدوار الأربعة (اختبار فعلي)
- [ ] حذف الحساب + تصدير البيانات (GDPR/متطلبات المتاجر)
- [ ] سياسة خصوصية وشروط حقيقية مربوطة
- [ ] إزالة الأزرار الميتة (Apple/Google) أو تفعيلها
- [ ] اختبار على أجهزة صغيرة + لوحية + iOS/Android
- [ ] مراقبة أخطاء (Sentry) + Rate limiting + مكافحة الحسابات الوهمية
- [ ] مراجعة أمنية خارجية (Pen-test) بعد إصلاح C-1..C-6

---

## 🚫 11) هل التطبيق جاهز للإطلاق؟

**لا.** رغم اتساع الواجهات وجودتها البصرية، فإن أيّ ثغرة من C-1 إلى C-6 وحدها كافية لمنع الإطلاق:
- **C-1** تجعل كل حساب قابلاً للاختراق بمعرفة رقم الجوال — كارثة خصوصية وأمن.
- **C-3/C-5** تسمحان بالاستيلاء الإداري وسرقة العمولات.
- **C-4** تسرّب عناوين ومحادثات المستخدمين.
- **C-6** تسمح بالشراء بمبالغ مزيّفة.
- إضافةً إلى ذلك، عدم تطابق الكود مع المخطط (H-1..H-11) سيجعل شاشات أساسية (الرئيسية، البحث، المحادثات، التقييمات، الكوبونات، إنشاء المتجر) **تتعطّل فور الاتصال بقاعدة بيانات حقيقية**.

الحالة الأصدق: **نموذج أوّلي (Prototype) متقدّم الواجهات**، وليس منتجاً جاهزاً للإنتاج.

---

## 📦 12) ما يجب إضافته قبل النشر على المتاجر (App Store / Google Play)

- **حذف الحساب داخل التطبيق** (شرط إلزامي لدى Apple وGoogle).
- سياسة خصوصية وشروط استخدام منشورة برابط عام + شاشة موافقة.
- إزالة/تفعيل أزرار تسجيل الدخول الاجتماعي (Apple Sign-In إلزامي إن وُجد أي مزوّد اجتماعي آخر على iOS).
- أذونات واضحة (موقع/إشعارات/كاميرا) مع نصوص مبرِّرة في `Info.plist`/manifest.
- مراقبة الأعطال (Crash reporting) ورقم إصدار وسياسة تحديث.
- شاشة "الإبلاغ عن محتوى/مستخدم" وحظر (لأي تطبيق فيه محتوى/محادثات من المستخدمين).
- التزام أمني: تشفير أثناء النقل (مضمون عبر Supabase HTTPS)، وعدم تخزين أسرار في الكود.

---

## 🏆 13) ميزات تجعله ينافس أمازون / نون / تمو

**ركائز الثقة والتحويل:** تتبّع طلب حيّ على الخريطة، إرجاع سلس بنقرة، ضمان استرداد، تقييمات موثّقة بصور، مقاس/توصية ذكية.
**نمو وتفاعل (نموذج تمو):** عروض ومض (Flash sales) بعدّاد، شراء جماعي، عجلة حظ/كوبونات يومية، إحالة بمكافآت، Live Shopping.
**تخصيص (نموذج أمازون):** توصيات بالذكاء الاصطناعي، "اشترِ مجدداً"، بحث نصي وصوتي وبالصورة، إشعارات ذكية لانخفاض السعر/عودة التوفّر.
**دفع محلي (سياق يمني):** تكامل حقيقي مع المحافظ اليمنية (جوالي/كريمي/فلوسك) + الدفع عند الاستلام مع تحصيل موثّق للمندوب + محفظة داخل التطبيق.
**تميّز تشغيلي:** أسطول توصيل مُدار بإسناد ذكي، تعدّد لغات (عربي/إنجليزي)، وصولية (Accessibility)، أداء عالٍ (كاش + صور مُحسّنة).
**ولاء:** نقاط، مستويات عضوية، اشتراك شحن مجاني (نموذج Prime).

> الأولوية العملية: **لا يُبنى أيٌّ من هذا قبل إغلاق C-1..C-6.** الأساس الأمني والمالي أولاً، ثم ميزات المنافسة.
