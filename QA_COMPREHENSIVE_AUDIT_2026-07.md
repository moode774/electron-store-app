# 🔍 تقرير الفحص الشامل قبل الإطلاق — تطبيق Marketplace

> تاريخ الفحص: 2026-07-02 · الفاحص: مراجعة QA + أمن + منتج + تجربة مستخدم

> ⚠️ **تصحيح لاحق بعد فحص قاعدة البيانات الحيّة (Supabase MCP):**
> هذا التقرير كُتب أولاً اعتماداً على ملفات الـ migration في المستودع، وهي
> **قديمة ولا تطابق القاعدة الحيّة**. بعد الفحص المباشر تبيّن:
> - **C3 (كشف البيانات الجماعي) غير موجود فعلياً** — RLS مفعّل بسياسات على كل
>   الجداول الحسّاسة في القاعدة الحيّة. تجاهل C3 أدناه.
> - **C4 (تصعيد الصلاحيات) و C6 (التلاعب بالأسعار) كانا حقيقيين** وقد **أُصلحا
>   وتُحقّق منهما على الحيّ**. راجع `SECURITY_FIXES_2026-07.md`.
> نوع المشروع الفعلي: **Expo / React Native (وليس Electron رغم اسم المستودع)** — Monorepo، الخلفية **Supabase (PostgreSQL) وليست Firebase**.
> نطاق الكود المفحوص: ~15,400 سطر، 44 شاشة، 5 ملفات migration، طبقة `api.ts` واحدة.

---

## ⚠️ الخلاصة التنفيذية أولاً

**نسبة الجاهزية للإطلاق: ~35–40% — التطبيق غير جاهز للإطلاق إطلاقاً في وضعه الحالي.**

الواجهات (UI) متقدمة ومكتملة تقريباً، لكن **طبقة الأمان والبيانات مكسورة بشكل خطير**. توجد ثلاث كوارث أمنية أي واحدة منها كافية لمنع الإطلاق:

1. **تجاوز كامل للمصادقة**: كلمة المرور مشتقة رياضياً من رقم الهاتف، فأي شخص يعرف رقم هاتفك يدخل حسابك.
2. **تسريب بيانات ضخم**: 17 جدولاً بلا أي حماية (RLS)، بينها **العناوين، الملفات الشخصية، والمحادثات الخاصة** — مكشوفة بالكامل لأي شخص يملك مفتاح التطبيق العام (وهو مضمّن داخل التطبيق).
3. **تصعيد صلاحيات**: أي عميل يستطيع ترقية نفسه إلى "تاجر" أو "أدمن" بتعديل حقل واحد.

بالإضافة إلى أن **دورة الطلب نفسها لا تعمل** مقابل قاعدة البيانات الحقيقية (سياسات RLS تمنع إنشاء/تحديث الطلبات)، و**لا توجد لوحة إدارة (Admin)** أصلاً رغم أنها قسم كامل في المتطلبات، و**لا يوجد أي اختبار آلي واحد**.

---

## 1️⃣ الأخطاء والثغرات المكتشفة (مصنّفة حسب الخطورة)

### 🔴 حرجة (Critical) — تمنع الإطلاق

#### C1 — تجاوز كامل للمصادقة (Authentication Bypass)
- **الملف:** `packages/shared-hooks/src/useAuthStore.ts:34-36, 69-81`
- كلمة المرور تُشتق حرفياً من الرقم:
  ```ts
  phoneToEmail(phone)    = `u${digits}@levi-phone.app`
  phoneToPassword(phone) = `Levi-${digits}-auth`   // ← متوقّعة 100%
  ```
- `signInWithPhone` يستدعي `signInWithPassword` بهذه الكلمة المتوقّعة **بدون أي OTP**.
- **الأثر:** أي مهاجم يعرف رقم هاتف ضحية (عميل/تاجر/مندوب/أدمن) يستطيع توليد كلمة المرور والدخول إلى حسابه فوراً. رقم الهاتف ليس سراً. هذا اختراق كامل لكل الحسابات في النظام.
- **السيناريو الأسوأ:** مهاجم يجرّب أرقاماً متسلسلة → يستحوذ على حسابات تجار، يسحب أرصدتهم، يغيّر أسعارهم، يقرأ محادثاتهم.

#### C2 — شاشة OTP وهمية وقابلة للتجاوز
- **الملف:** `apps/customer/src/screens/auth/OtpScreen.tsx:106-109`
- لا يُرسَل أي SMS. زر "إعادة الإرسال" يستدعي `signInWithPhone(phone)` الذي **يسجّل الدخول مباشرة بلا رمز**. أي: افتح الشاشة، اضغط "إعادة الإرسال" → دخلت بلا كتابة أي رمز.
- `verifyOtp` الحقيقي (SMS) موجود لكنه **غير مستخدم** في المسار الفعلي.
- **الأثر:** طبقة التحقق كلها ديكور. لا يوجد تحقق من ملكية الرقم إطلاقاً.

#### C3 — 17 جدولاً بلا حماية RLS (تسريب بيانات جماعي)
- الجداول التالية **لم تُفعّل عليها RLS في أي migration**، ومع منح Supabase صلاحيات SELECT/INSERT الافتراضية لدور `anon`/`authenticated`، فهي **مقروءة/قابلة للكتابة من أي شخص يملك المفتاح العام** (المضمّن داخل حزمة التطبيق):
  `addresses`, `customer_profiles`, `chat_conversations`, `chat_messages`, `notifications`, `carts`, `cart_items`, `wishlists`, `refund_requests`, `product_images`, `product_variants`, `advertisements`, `complaints`, `order_tracking`, `platform_settings`, `inventory_logs`, `store_follows`.
- **الأخطر بينها:**
  - `addresses` → **عناوين منازل كل العملاء + إحداثيات GPS** (بيانات موقع حسّاسة).
  - `customer_profiles` → بيانات شخصية + نقاط الولاء (قابلة للتلاعب).
  - `chat_conversations` + `chat_messages` → **كل المحادثات الخاصة بين كل العملاء وكل التجار**.
  - `platform_settings` → إعدادات المنصة (عمولات، تهيئة) — إن كانت قابلة للكتابة، يعبث بها المهاجم.
- **الأثر:** خرق خصوصية كارثي ومخالفة صريحة لسياسات متاجر Apple/Google وأنظمة حماية البيانات.

#### C4 — تصعيد الصلاحيات (Privilege Escalation)
- **الملف:** `supabase/migrations/20260612000000_initial_rls.sql:17-19`
- سياسة `users` تسمح للمستخدم بتحديث سجله `WITH CHECK (auth.uid() = id)`، والتعليق نفسه يقول *"يجب منع تعديل حقل الدور عبر trigger"* — **لكن هذا الـ trigger غير موجود في أي migration.**
- الأدوار مخزّنة في `users.role`، والتوجيه في `App.tsx` مبني عليها بالكامل (Client-side).
- **الأثر:** أي عميل يرسل `update users set role='merchant'` (أو `'admin'`) على سجله → يفتح واجهة التاجر/الإدارة. تجاوز كامل لنظام الأدوار.

#### C5 — دورة الطلب لا تعمل مقابل قاعدة البيانات الحقيقية
- **الملفات:** `packages/shared-hooks/src/api.ts:404, 448` مقابل `supabase/migrations/20260612000000_initial_rls.sql:52`
- جدول `orders` عليه سياسة **SELECT فقط**، ولا توجد أي سياسة **INSERT أو UPDATE**. مع تفعيل RLS، القاعدة الافتراضية = **منع**.
- لكن `createOrder` و `updateOrderStatus` يُنفّذان `insert`/`update` **مباشرة على الجدول من العميل** → سيرفضهما RLS.
- **الأثر:** إنشاء الطلب، قبول التاجر، تغيير الحالة، إسناد المندوب — **كل دورة الطلب معطّلة فعلياً**. التصميم المقصود كان عبر RPC/Edge Function لكن الكود يكتب مباشرة → تعارض معماري.

#### C6 — التلاعب بالأسعار والإجماليات (Price Tampering)
- **الملف:** `apps/customer/src/screens/main/cart/CheckoutScreen.tsx:69-86` + `api.ts:383-446`
- الإجماليات كلها تُحسب في العميل وتُرسل كما هي: `subtotal`, `total_amount`, `discount_amount`, `delivery_fee` (من ثابت في العميل)، والأسعار من `useCartStore` (يحتفظ بالسعر محلياً).
- **الأثر:** عميل يعدّل التطبيق يرسل `total_amount = 0` أو سالباً أو خصماً عشوائياً. لا يوجد أي تحقق من الأسعار في الخادم (`calculate_order_totals` غير موجودة في الـ migrations). بمجرد إضافة سياسة INSERT، تصبح الثغرة حيّة.

#### C7 — انحراف المخطط (Schema Drift) — الـ migrations لا تُعيد إنتاج القاعدة
- **الملفات:** `20260619000001...sql` يمنح صلاحيات لدوال (`decrement_product_stock`, `claim_delivery_order`, `calculate_order_totals`) و RLS لـ ~50 جدولاً (`admin_roles`, `badges`, `fraud_flags`...) **غير معرّفة في migration المخطط الأساسي** (32 جدولاً فقط، ودالة `handle_new_user` فقط).
- **الأثر:** القاعدة الحيّة عُدّلت يدوياً خارج نظام التحكم بالإصدارات. أي نشر جديد من الـ migrations **لن ينتج قاعدة عاملة** — استدعاءات `rpc('decrement_product_stock')` و`rpc('claim_delivery_order')` ستفشل. لا يمكن إعادة بناء البيئة بشكل موثوق.

---

### 🟠 عالية (High)

- **H1 — الكوبونات معطّلة كلياً في العميل:** سياسة `coupons` هي `USING (false)` (`initial_rls.sql:87`)، بينما `validateCoupon`, `getActiveCoupons`, `getAccountStats` تستعلم الجدول مباشرة من العميل → دائماً فارغ/مرفوض. النتيجة: كل كود خصم يُرفض بـ"كود غير صحيح"، شاشة العروض فارغة، وعدّاد الكوبونات = 0.
- **H2 — لا يوجد فرض لاستخدام الكوبون:** جدول `coupon_usage` موجود لكنه لا يُكتب أبداً. لا حد أقصى للاستخدام ولا حد لكل مستخدم → إعادة استخدام لا نهائية عند تشغيل الكوبونات.
- **H3 — لا ذرّية (Atomicity) في إنشاء الطلب:** `createOrder` ينفّذ 3 عمليات منفصلة (إدراج الطلب، إدراج العناصر، خصم المخزون) بلا معاملة (transaction). فشل جزئي → طلبات/عناصر يتيمة أو مخزون غير متطابق.
- **H4 — لا منع للبيع الزائد (Oversell):** الطلب يُنشأ ثم يُخصم المخزون بلا تحقق مسبق من التوفّر؛ لا ضمان أن `decrement_product_stock` يمنع القيم السالبة. سيناريو نفاد المخزون غير مُدار.
- **H5 — السلة متعددة المتاجر بلا تراجع:** الدفع ينشئ طلباً لكل متجر داخل حلقة، ثم يُفرّغ السلة. فشل أحد الطلبات في المنتصف يترك طلبات مكتملة جزئياً وسلة مفرّغة.
- **H6 — لا توجد لوحة إدارة (Admin) إطلاقاً:** كامل قسم "الإدارة" في المتطلبات (المستخدمون، المتاجر، العمولات، البلاغات، البنرات، الصلاحيات، النسخ الاحتياطي، السجلات) **غير مبني**. دور `admin` في `App.tsx` يعرض واجهة العميل العادية فقط.
- **H7 — لا يوجد أي اختبار آلي ولا CI:** صفر ملفات اختبار، لا مجلد `.github/workflows`. منتج تجاري بـ15 ألف سطر يتعامل مع مدفوعات بلا أي شبكة أمان.
- **H8 — تسجيل الدخول عبر Google/Apple غير مُنفّذ:** الأزرار موجودة بلا وظيفة (مذكور في `audit_report.md`).
- **H9 — الاسترجاع/الإلغاء بلا سير عمل مالي:** `cancelOrder` يحدّث `orders` مباشرة (يرفضه RLS)، و`createRefundRequest` يكتب في `refund_requests` المكشوف (C3) بلا موافقة ولا حركة أموال ولا إعادة مخزون.

---

### 🟡 متوسطة (Medium)

- **M1 — لا تعدد لغات:** لا توجد مكتبة i18n؛ النصوص عربية ثابتة والـRTL مثبّت في الكود. متطلب "اللغة" غير محقّق.
- **M2 — لا وضع ليلي (Dark Mode):** لا نظام Theme؛ الألوان ثوابت.
- **M3 — لا خرائط:** `react-native-maps` غير مثبّتة؛ تتبّع/تحديد موقع التوصيل عبارة عن placeholder.
- **M4 — لا إشعارات فورية (Push):** جدول `device_tokens` موجود لكن `expo-notifications` غير مثبّتة ولا تُسجَّل الرموز. "الإشعارات" شاشة قراءة من قاعدة البيانات فقط.
- **M5 — رفع الصور غير آمن:** `uploadImageToStorage` يستخدم `upsert: true` مع مسار من المُستدعي على bucket عام، بلا تحقق من الحجم/النوع في الخادم → احتمال الكتابة فوق صور آخرين / تصادم أسماء.
- **M6 — البحث بالعربية معطوب جزئياً:** `searchProducts` يبحث في `name` فقط (وليس `name_ar`) → أسماء المنتجات العربية لا تظهر. **البحث الصوتي غير موجود إطلاقاً.**
- **M7 — التصنيفات الفرعية لا تظهر:** `getCategories` يجلب المستوى الأعلى فقط (`parent_id is null`)؛ لا تنقّل شجري.
- **M8 — حذف الحساب غير مُنفّذ:** لا دالة حذف حساب؛ نص `LegalScreen` يحيل إلى "التواصل مع الدعم". هذا متطلب صريح في متاجر التطبيقات (Apple/Google).
- **M9 — معالجة الأخطاء متذبذبة:** بعض الدوال ترمي (`throw`) وقد لا تُلتقط في الشاشات → انهيار؛ وأخرى تبتلع الخطأ وتُرجع `[]` فتُخفي الأعطال عن المستخدم والمطوّر.
- **M10 — لا Rate Limiting ولا مكافحة احتيال فعلية:** جدول `fraud_flags` موجود لكن بلا منطق؛ لا حماية من الحسابات الوهمية أو السبام (يتفاقم مع C1/C2).
- **M11 — تصادم هوية الهاتف:** `phoneDigits` يجرّد كل غير الأرقام، فـ`+967 1 234` و`0096712 34` قد يتطابقان → خلط حسابات/تعارض بريد.

---

### 🟢 منخفضة (Low)

- **L1** — `start-web.js` يثبّت مساراً ويندوزياً `C:/Users/mode/...` → معطّل على أي جهاز آخر/بيئة CI.
- **L2** — `README.md` يصف "تطبيق سطح مكتب Electron" بينما هو Expo React Native — مضلّل.
- **L3** — `tax_amount` دائماً 0 ورسوم التوصيل من خريطة ثابتة في العميل (لا تُدار من الخادم).
- **L4** — `audit_report.md` يقول "TypeScript نظيف 100%" لكن لا CI يفرض ذلك؛ يعتمد على الذاكرة.
- **L5** — `console.error` يسرّب تفاصيل تهيئة/أخطاء في الإنتاج.

---

## 2️⃣ النواقص (ميزات مطلوبة وغير موجودة)

| الميزة | الحالة | لماذا مهمة |
|---|---|---|
| لوحة إدارة (Admin) كاملة | غير موجودة | لا يمكن تشغيل سوق دون إدارة للمستخدمين/المتاجر/البلاغات/العمولات |
| مزوّد SMS/OTP حقيقي | غير موجود | حجر أساس المصادقة والثقة |
| تسجيل Google/Apple | أزرار فقط | مطلب فعلي + شرط Apple عند وجود دخول اجتماعي |
| حذف الحساب داخل التطبيق | غير موجود | **شرط إلزامي في App Store / Google Play** |
| الإشعارات الفورية (Push) | غير موجودة | جوهر تجربة الطلب واللحظية |
| الخرائط وتتبّع المندوب المباشر | placeholder | جوهر تجربة التوصيل |
| تعدد اللغات + الوضع الليلي | غير موجودين | متطلبات صريحة |
| البحث الصوتي | غير موجود | متطلب صريح |
| سير عمل الاسترجاع/الاستبدال المالي | ناقص | ضروري لثقة العميل والامتثال |
| المدفوعات الإلكترونية | COD فقط | لا بوابة دفع حقيقية |
| الاختبارات الآلية + CI | غير موجودة | لا يمكن ضمان الجودة قبل الإطلاق |

---

## 3️⃣ المخاطر

- **قانوني/امتثال:** كشف العناوين والمحادثات (C3) = مخالفة خصوصية قد تؤدي لغرامات ورفض من المتاجر.
- **مالي:** التلاعب بالأسعار (C6) + إعادة استخدام الكوبونات (H2) + الاستحواذ على حسابات التجار وأرصدتهم (C1).
- **سمعة:** أي تسريب مبكر يدمّر ثقة المستخدمين في منصة ناشئة.
- **تشغيلي:** انحراف المخطط (C7) يجعل النشر/الاستعادة غير موثوقين — خطر توقّف يصعب إصلاحه.

---

## 4️⃣ التحسينات المقترحة (بعد إصلاح الحرج)

1. نقل كل الكتابات الحسّاسة (إنشاء الطلب، تغيير الحالة، الكوبونات، الأرصدة) إلى **Edge Functions / RPC آمنة** مع تحقق كامل من الأسعار في الخادم.
2. إعادة كتابة الـ migrations لتعكس القاعدة الحيّة بالكامل (Single Source of Truth) + إضافة `supabase db reset` في CI.
3. إضافة طبقة i18n وTheme من البداية.
4. Realtime لعروض المندوب وحالة الطلب والمحادثات.
5. Pagination وLazy Loading للقوائم (منتجات/طلبات).
6. مراقبة أخطاء (Sentry) وتحليلات.

---

## 5️⃣ خطة الإصلاح مرتّبة حسب الأولوية

**المرحلة 0 — إيقاف النزيف (قبل أي شيء):**
1. استبدال مصادقة الهاتف بـ OTP حقيقي عبر مزوّد SMS (إزالة اشتقاق كلمة المرور نهائياً) — يحل C1، C2.
2. تفعيل RLS وكتابة سياسات دقيقة لكل الجداول الـ17 المكشوفة — يحل C3.
3. إضافة trigger يمنع تعديل `role` (والحقول الحسّاسة) في `users` — يحل C4.

**المرحلة 1 — دورة الطلب:**
4. RPC/Edge Function واحدة لإنشاء الطلب: تحقق أسعار من الخادم + خصم مخزون + منع البيع الزائد داخل معاملة واحدة — يحل C5، C6، H3، H4، H5.
5. RPC للكوبونات مع فرض الاستخدام (`coupon_usage`) — يحل H1، H2.
6. سياسات UPDATE محكومة بالحالة والدور للطلبات (من يملك أي انتقال حالة).

**المرحلة 2 — إعادة إنتاج البيئة:**
7. مزامنة الـmigrations مع القاعدة الحيّة (C7) + CI يشغّل `db reset` + typecheck + lint.

**المرحلة 3 — النواقص الإلزامية للمتاجر:**
8. حذف الحساب داخل التطبيق (M8)، سياسة خصوصية فعلية، Google/Apple Sign-In، Push.

**المرحلة 4 — لوحة الإدارة + التوصيل:**
9. بناء لوحة Admin (H6)، الخرائط وتتبّع المندوب، سير الاسترجاع المالي (H9).

**المرحلة 5 — الجودة والتلميع:**
10. اختبارات آلية، i18n، Dark Mode، البحث بالعربية/الصوتي، بوابة دفع.

---

## 6️⃣ Checklist ما قبل الإطلاق

- [ ] OTP/SMS حقيقي — إزالة كلمة المرور المشتقة (C1/C2)
- [ ] RLS مفعّل + مُختبَر على **كل** جدول (C3)
- [ ] منع تعديل الدور والحقول الحسّاسة (C4)
- [ ] إنشاء الطلب عبر RPC مع تحقق أسعار خادمي ومعاملة ذرّية (C5/C6/H3/H4)
- [ ] كوبونات آمنة مع فرض الاستخدام (H1/H2)
- [ ] migrations تُعيد إنتاج القاعدة بالكامل + CI أخضر (C7/H7)
- [ ] حذف الحساب داخل التطبيق (M8)
- [ ] سياسة خصوصية وشروط فعلية منشورة
- [ ] Push Notifications تعمل لكل الأدوار
- [ ] لوحة إدارة تشغيلية (H6)
- [ ] بوابة دفع + سير استرجاع مالي (H9)
- [ ] اختبار اختراق مستقل بعد الإصلاحات
- [ ] اختبارات آلية للمسارات الحرجة (دفع، طلب، مصادقة)
- [ ] مراقبة أخطاء (Sentry) + تحليلات

---

## 7️⃣ هل التطبيق جاهز للإطلاق؟

**لا.** ثلاث ثغرات حرجة (تجاوز مصادقة، كشف بيانات جماعي، تصعيد صلاحيات) كل واحدة كافية لرفض التطبيق أخلاقياً وقانونياً ومن متاجر التطبيقات. يُضاف إليها أن **دورة الطلب الأساسية لا تعمل** مقابل القاعدة الحقيقية، **لا توجد لوحة إدارة**، و**لا اختبارات**. الواجهات ممتازة لكن الأساس (الأمان + البيانات) يحتاج إعادة بناء لطبقة الكتابة قبل التفكير في الإطلاق.

## 8️⃣ ما الذي يجب إضافته قبل النشر على المتاجر (Apple/Google)

- **حذف الحساب داخل التطبيق** (شرط إلزامي).
- سياسة خصوصية عاملة + إفصاح البيانات (App Privacy / Data Safety).
- تصحيح كل ثغرات الخصوصية (C3) قبل أي مراجعة.
- Apple Sign-In إن وُجد دخول اجتماعي آخر.
- أذونات الموقع/الإشعارات مع تبريرها.
- إزالة أي "أبواب خلفية" تطويرية (رموز/كلمات مرور تجريبية).
- شاشات فارغة/أخطاء مهذّبة بدل الانهيار.

## 9️⃣ ميزات تجعله ينافس أمازون/نون/تمو

- بحث ذكي (عربي + صوتي + توصيات مبنية على السلوك) وترتيب مخصّص.
- توصيل بتتبّع مباشر على الخريطة + ETA دقيق.
- برنامج ولاء وإحالات فعّال (الجداول موجودة، المنطق ناقص).
- عروض فلاش/مزادات/تجميع طلبات (Group Buy مثل تمو).
- محفظة داخلية + بوابات دفع متعددة + تقسيط.
- تقييمات موثّقة بالشراء + صور/فيديو.
- مركز تاجر تحليلي متقدم + إدارة مخزون احترافية.
- دعم عملاء لحظي (شات + بوت) وإدارة نزاعات عادلة.

---

### ملحق: الأدلة المرجعية السريعة
| المعرّف | الملف والسطر |
|---|---|
| C1 | `packages/shared-hooks/src/useAuthStore.ts:34-36, 69-81` |
| C2 | `apps/customer/src/screens/auth/OtpScreen.tsx:106-109` |
| C3 | 17 جدولاً بلا `ENABLE ROW LEVEL SECURITY` في `supabase/migrations/*` |
| C4 | `supabase/migrations/20260612000000_initial_rls.sql:17-19` |
| C5 | `initial_rls.sql:52` مقابل `api.ts:404, 448` |
| C6 | `apps/customer/src/screens/main/cart/CheckoutScreen.tsx:69-86` |
| C7 | `20260619000001_secure_views_functions_and_rpc_grants.sql` مقابل `20260610000000_initial_schema.sql` |
| H1 | `initial_rls.sql:87` مقابل `api.ts:954-993` |
</content>
</invoke>
