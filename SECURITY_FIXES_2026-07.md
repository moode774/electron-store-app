# 🔧 إصلاحات الأمان والسلامة — 2026-07-02

هذا المستند يوضّح ما **تم إصلاحه في الكود** استجابةً لتقرير الفحص
(`QA_COMPREHENSIVE_AUDIT_2026-07.md`)، وما **يتطلب إجراءً منك** لتفعيل الإصلاح.

---

## ✅✅ تم التطبيق والتحقق على قاعدة البيانات الحيّة (عبر Supabase MCP)

بعد ربط قاعدتكم الحيّة، فُحِصت وطُبّقت الإصلاحات وتأكّدت فعلياً:

- **تصحيح مهم لتقرير الفحص:** ثغرة **C3 (كشف البيانات) غير موجودة على قاعدتكم الحيّة** — الـRLS مفعّل بسياسات على كل الجداول الحسّاسة (addresses: سياستان، chat: 4، orders: 7، coupons: 5...). كان استنتاجي السابق مبنياً على ملفات migration قديمة لا تطابق القاعدة الحيّة. أعتذر عن المبالغة وقد صُحِّحت.
- **C4 (تصعيد الصلاحيات) كان حقيقياً وأُصلح وتُحقّق منه:** أضيف trigger يمنع تغيير الدور؛ اختُبِر بمحاولة `UPDATE users SET role='admin'` → بقي `customer`. ✅
- **C6 (التلاعب بالأسعار) كان حقيقياً وأُصلح وتُحقّق منه:** كانت هناك سياسة `orders_customer_insert` + دالة `create_order_with_items` تثق بمبالغ العميل. أضيفت triggers تُعيد حساب الأسعار/الإجماليات وتتحقق من الخصم. اختُبِر بطلب مزوّر (سعر 1، خصم 9999) → صُحّح تلقائياً (السعر 349، الخصم 0، الإجمالي الصحيح). ✅
- **إغلاق دوال SECURITY DEFINER المكشوفة لـ anon** (`create_order_with_items`, `merchant_top_products`, `bump_merchant_daily_stats`, `notify_couriers_on_ready`). ✅
- **دوال آمنة جديدة على الحيّ:** `place_order`, `preview_coupon`, `set_order_status`, `delete_my_account`, ودوال الأدمن + سياسات الأدمن. جميعها `anon=false / authenticated=true`. ✅

> ملاحظة: ملفات `supabase/migrations/20260702000000` و`...0001` حُدِّثت لتطابق **حرفياً** ما طُبّق على الحيّ (بأسماء أعمدة القاعدة الحيّة الحقيقية).

### يبقى إعداداً منك على مستوى المصادقة/الحساب
- **مزوّد SMS** لتفعيل OTP (Auth → Phone).
- تفعيل **Leaked Password Protection** (Auth) — تحذير من مستشار Supabase.
- إنشاء حساب أدمن: `UPDATE users SET role='admin' WHERE id='...';` (لم يعد بالإمكان فعلها من العميل).

---

## ✅ ما تم إصلاحه في الكود

| # | المشكلة (من التقرير) | الإصلاح | الملفات |
|---|---|---|---|
| C1 | تجاوز المصادقة (كلمة مرور مشتقّة من الهاتف) | إزالة الاشتقاق نهائياً واستبداله بـ **OTP حقيقي عبر SMS** (`signInWithOtp`/`verifyOtp`) | `packages/shared-hooks/src/useAuthStore.ts` |
| C2 | شاشة OTP وهمية / زر إعادة الإرسال يسجّل الدخول | Login/Register يرسلان الرمز ثم ينتقلان لشاشة الرمز؛ التحقق حقيقي؛ "إعادة الإرسال" تُعيد إرسال OTP فعلياً | `LoginScreen.tsx`, `RegisterScreen.tsx`, `OtpScreen.tsx` |
| C3 | 17 جدولاً بلا RLS | migration يفعّل RLS + سياسات ملكية دفاعية على كل الجداول المكشوفة | `supabase/migrations/20260702000000_*.sql` |
| C4 | تصعيد الصلاحيات (تعديل الدور ذاتياً) | trigger `protect_user_sensitive_fields` يمنع تعديل `role/is_active/is_verified/admin_role_id` لغير الأدمن + منع طلب دور admin عند التسجيل | migration + `useAuthStore.ts` |
| C5 | دورة الطلب مرفوضة من RLS | دوال آمنة `place_order` و`set_order_status` (SECURITY DEFINER) بضوابط دور وانتقالات | migration + `api.ts` |
| C6 | التلاعب بالأسعار | الأسعار/الإجماليات/رسوم التوصيل تُحسب **في الخادم فقط**؛ العميل لا يرسل أي مبالغ | `place_order` + `CheckoutScreen.tsx` + `api.ts` |
| H1/H2 | الكوبونات معطّلة/بلا فرض | `preview_coupon` للمعاينة + فرض الاستخدام داخل `place_order` (تحقق من الصلاحية/الحد الأدنى/العدد/الاستخدام لكل مستخدم عبر `coupon_usage`) | migration + `api.ts` |
| H3/H4/H5 | لا ذرّية / بيع زائد | إنشاء الطلب كله داخل دالة واحدة (معاملة) مع قفل صفوف المنتجات وخصم مخزون يمنع السالب | `place_order` |
| M6 | البحث لا يشمل العربية | البحث الآن في `name` و`name_ar` | `api.ts` |
| M7 | التصنيفات الفرعية لا تظهر | `getCategories(topLevelOnly)` + `getSubcategories()` | `api.ts` |
| M8 | لا حذف حساب | زر "حذف الحساب نهائياً" + دالة `delete_my_account` | `AccountScreen.tsx`, `useAuthStore.ts`, migration |
| L1 | مسار ويندوز ثابت | `start-web.js` أصبح محمولاً | `start-web.js` |
| L2 | README مضلّل | تحديثه ليصف المشروع بدقة | `README.md` |

---

## ⚠️ ما يتطلب إجراءً منك (لا يمكن إتمامه من الكود وحده)

1. **تفعيل مزوّد SMS** في Supabase → Authentication → Phone (Twilio/Vonage/MessageBird).
   بدونه لن تُرسَل رموز OTP. هذا شرط تشغيل المصادقة الجديدة.
2. **تطبيق الـ migration** على قاعدة بياناتك:
   ```bash
   supabase db push
   ```
   ⚠️ قاعدتك الحيّة انحرفت عن ملفات الـ migration (أسماء أعمدة مختلفة في
   `coupons`/`chat`/`reviews`...). الملف كُتب دفاعياً ليتجاوز غير الموجود، لكن
   **طبّقه على فرع staging أولاً وراجع رسائل NOTICE** للتأكد من عدم تخطّي سياسة مهمة
   بسبب اختلاف اسم عمود. عدّل أسماء الأعمدة عند الحاجة لتطابق قاعدتك.
3. **إضافة إعداد رسوم التوصيل** (اختياري): صف في `platform_settings`
   بمفتاح `default_delivery_fee` وقيمة رقمية، وإلا تُستخدم 1500 افتراضياً.
4. **مواءمة المخطط**: يُنصح بإعادة توليد ملفات الـ migration من القاعدة الحيّة
   (`supabase db diff`) لتصبح مصدر الحقيقة الوحيد ويعمل النشر/الاستعادة بموثوقية.

---

## 🆕 ميزات أُضيفت (الدفعة الثانية — رفع الجاهزية)

| الميزة | الوصف | الملفات |
|---|---|---|
| **لوحة إدارة (Admin)** | تبويبات: لوحة تحكم بإحصائيات حيّة، إدارة المستخدمين (إيقاف/تفعيل)، اعتماد المتاجر، عرض الطلبات بالحالة، الإعدادات. محميّة بـ RLS ودوال `admin_*` تتحقق من `is_admin()` | `screens/admin/*`, `navigation/AdminTabNavigator.tsx`, `adminApi.ts`, migration `..._admin_access_and_push.sql` |
| **تعدد اللغات (ar/en)** | قاموس ترجمة + متجر إعدادات مُخزَّن + `t()` تفاعلي | `shared-utils/i18n.ts`, `useSettingsStore.ts`, `screens/shared/SettingsScreen.tsx` |
| **الوضع الليلي** | لوحتا ألوان (فاتح/داكن) + مبدّل في الإعدادات + شاشات الأدمن تستخدم الألوان الديناميكية | `shared-utils/theme.ts`, `useSettingsStore.ts` |
| **الإشعارات الفورية (تسجيل الجهاز)** | خطاف `usePushNotifications` يسجّل رمز الجهاز في `device_tokens` (RLS بالملكية). يتخطّى بهدوء إن لم تُثبَّت الحزمة | `usePushNotifications.ts`, migration |
| **شاشة الإعدادات** | لغة + مظهر + تسجيل خروج + حذف حساب، متاحة للعميل والأدمن | `SettingsScreen.tsx` |

### خطوات تفعيل مطلوبة منك
- **الإشعارات الفورية:** `npx expo install expo-notifications expo-device`، ثم بناء خادم إرسال (Expo Push API) يقرأ من `device_tokens`.
- **حساب أدمن:** لا يمكن إنشاؤه من التسجيل (أمان). عيّنه يدوياً في قاعدة البيانات: `UPDATE users SET role='admin' WHERE id='...';`
- **الإنجليزية الكاملة (LTR):** الترجمة جاهزة؛ قلب الاتجاه لـ LTR يتطلب إعادة تشغيل التطبيق (`I18nManager.forceRTL(false)`).

## 🚧 ما لم يُنجز بعد (يحتاج خدمات خارجية أو مشاريع مستقلة)

- **بوابة دفع إلكترونية** حقيقية (حالياً COD فقط) — تحتاج مزوّداً ومفاتيح.
- **الخرائط وتتبّع المندوب المباشر** (`react-native-maps` + مفتاح خرائط).
- **البحث الصوتي**.
- **مجموعة اختبارات آلية + CI**.
- **تسجيل Google/Apple** (يتطلب إعداد OAuth في Supabase + معرّفات العميل).
- **توسعة الترجمة** لتغطية كل الشاشات (البنية جاهزة، يتبقى إضافة المفاتيح تدريجياً).

راجع خطة الإصلاح المرحلية في التقرير الأساسي.
